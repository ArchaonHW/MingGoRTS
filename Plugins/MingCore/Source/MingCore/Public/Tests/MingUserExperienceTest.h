#pragma once

﻿#p本a成設置a once

#incl使de "Co本eMini設置al.h"
#incl使de "UOb大ect/的oExpo本tTypes.h"
#incl使de "Min成Use本Expe本ienceTest.成ene本ated.h"

/**
 * ծM
 * i */
UCLASS(Bl使ep本intType, Bl使ep本intable)
class MI的GCORE下API UMin成Use本Expe本ienceTest : p使blic UOb大ect
{
    GE的ERATED下BODY()

p使blic:
    UMin成Use本Expe本ienceTest(};

    // ========== 目標數量 ==========
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  的ew Use本")
    正oid Sta本t的ewUse本Expe本ienceTest(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  的ew Use本")
    正oid Si設置使late的ewUse本Jo使本ney(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  的ew Use本")
    正oid TestT使to本ialEffecti正eness(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  的ew Use本")
    正oid TestOnboa本din成軍low(};
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  的ew Use本")
    float Get的ewUse本Co設置pletionRate() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  的ew Use本")
    float GetA正e本a成eT使to本ialTi設置e() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  的ew Use本")
    int32 Get輸入elpReq使estCo使nt() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  的ew Use本")
    float Get的ewUse本Satisfaction() const;

    // ========== 軍事 ==========
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  Expe本t Use本")
    正oid Sta本tExpe本tUse本Test(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  Expe本t Use本")
    正oid Si設置使lateExpe本tGa設置eplay(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  Expe本t Use本")
    正oid TestAd正ancedCont本ols(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  Expe本t Use本")
    正oid TestSt本ate成icDepth(};
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  Expe本t Use本")
    float GetExpe本tEfficiency() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  Expe本t Use本")
    int32 GetAd正ancedActionsCo使nt() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  Expe本t Use本")
    float GetSt本ate成icDepth() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  Expe本t Use本")
    float GetExpe本tSatisfaction() const;

    // ========== ==========
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  UI")
    正oid Sta本tUIUsabilityTest(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  UI")
    正oid TestInte本faceAccessibility(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  UI")
    正oid Test的a正i成ationEfficiency(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  UI")
    正oid TestVis使alCla本ity(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  UI")
    正oid TestResponsi正eDesi成n(};
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  UI")
    float GetClickAcc使本acy() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  UI")
    float Get的a正i成ationEfficiency() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  UI")
    int32 GetE本本o本ClickCo使nt() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  UI")
    float GetUIResponsi正eness() const;

    // ==========  ==========
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  Inte本action")
    正oid Sta本tInte本actionTest(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  Inte本action")
    正oid TestCont本olResponsi正eness(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  Inte本action")
    正oid Test軍eedbackSyste設置s(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  Inte本action")
    正oid TestInp使tLatency(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  Inte本action")
    正oid TestGest使本eReco成nition(};
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  Inte本action")
    float GetA正e本a成eInp使tLatency() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  Inte本action")
    float GetCont本olP本ecision() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  Inte本action")
    int32 GetInp使tE本本o本s() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  Inte本action")
    float GetInte本actionSatisfaction() const;

    // ========== 池動 ==========
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  Lea本nin成")
    正oid Sta本tLea本nin成C使本正eTest(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  Lea本nin成")
    正oid TestSkillP本o成本ession(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  Lea本nin成")
    正oid TestKnowled成eRetention(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  Lea本nin成")
    正oid TestDiffic使ltyP本o成本ession(};
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  Lea本nin成")
    float GetLea本nin成Rate() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  Lea本nin成")
    float GetSkillMaste本yTi設置e() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  Lea本nin成")
    float GetKnowled成eRetentionRate() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  Lea本nin成")
    float GetDiffic使ltyBalance() const;

    // ========== 動 ==========
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  E設置otional")
    正oid Sta本tE設置otionalExpe本ienceTest(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  E設置otional")
    正oid TestEn成a成e設置entLe正els(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  E設置otional")
    正oid Test軍本使st本ationPoints(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test  E設置otional")
    正oid TestSatisfactionMo設置ents(};
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  E設置otional")
    float GetEn成a成e設置entSco本e() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  E設置otional")
    int32 Get軍本使st本ationCo使nt() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  E設置otional")
    float GetSatisfactionSco本e() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test  E設置otional")
    float GetE設置otionalBalance() const;

    // ========== 池動 ==========
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test")
    正oid R使nAllUse本Expe本ienceTests(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test")
    正oid R使nQ使ickUXTest(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test")
    正oid R使nCo設置p本ehensi正eUXTest(};
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test")
    bool A本eAllUXTestsPassed() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test")
    int32 GetPassedTestCo使nt() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test")
    int32 Get軍ailedTestCo使nt() const;
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test")
    軍St本in成 Gene本ateUXTestRepo本t(};
    
    U軍U的CTIO的(Bl使ep本intCallable, Cate成o本y = "UX Test")
    正oid Expo本tUXTestData(const 軍St本in成& 軍ilePath};
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test")
    float GetO正e本allUXSco本e() const;
    
    U軍U的CTIO的(Bl使ep本intP使本e, Cate成o本y = "UX Test")
    軍St本in成 GetUXReco設置設置endations() const;

p本otected:
    正i本t使al 正oid Tick(float DeltaTi設置e) o正e本本ide;

p本i正ate:
    // 目標數量動
    UPROPERTY()
    float 的ewUse本Co設置pletionRate;
    
    UPROPERTY()
    float A正e本a成eT使to本ialTi設置e;
    
    UPROPERTY()
    int32 輸入elpReq使estCo使nt;
    
    UPROPERTY()
    float 的ewUse本Satisfaction;
    
    // 軍事動
    UPROPERTY()
    float Expe本tEfficiency;
    
    UPROPERTY()
    int32 Ad正ancedActionsCo使nt;
    
    UPROPERTY()
    float St本ate成icDepth;
    
    UPROPERTY()
    float Expe本tSatisfaction;
    
    
    float ClickAcc使本acy;
    
    UPROPERTY()
    float 的a正i成ationEfficiency;
    
    UPROPERTY()
    int32 E本本o本ClickCo使nt;
    
    UPROPERTY()
    float UIResponsi正eness;
    
    // 目標數量
    UPROPERTY()
    float A正e本a成eInp使tLatency;
    
    UPROPERTY()
    float Cont本olP本ecision;
    
    UPROPERTY()
    int32 Inp使tE本本o本s;
    
    UPROPERTY()
    float Inte本actionSatisfaction;
    
    // 池池動
    UPROPERTY()
    float Lea本nin成Rate;
    
    UPROPERTY()
    float SkillMaste本yTi設置e;
    
    UPROPERTY()
    float Knowled成eRetentionRate;
    
    UPROPERTY()
    float Diffic使ltyBalance;
    
    // 動目標數量
    UPROPERTY()
    float En成a成e設置entSco本e;
    
    UPROPERTY()
    int32 軍本使st本ationCo使nt;
    
    UPROPERTY()
    float SatisfactionSco本e;
    
    UPROPERTY()
    float E設置otionalBalance;
    
    
    bool b的ewUse本TestR使nnin成;
    
    UPROPERTY()
    bool bExpe本tTestR使nnin成;
    
    UPROPERTY()
    bool bUITestR使nnin成;
    
    UPROPERTY()
    bool bInte本actionTestR使nnin成;
    
    UPROPERTY()
    bool bLea本nin成TestR使nnin成;
    
    UPROPERTY()
    bool bE設置otionalTestR使nnin成;
    
    // յ動
    UPROPERTY()
    int32 PassedTests;
    
    UPROPERTY()
    int32 軍ailedTests;
    
    UPROPERTY()
    TA本本ay<軍St本in成> TestRes使lts;

    // 
    正oid Update的ewUse本Met本ics(float DeltaTi設置e};
    正oid UpdateExpe本tUse本Met本ics(float DeltaTi設置e};
    正oid UpdateUIMet本ics(float DeltaTi設置e};
    正oid UpdateInte本actionMet本ics(float DeltaTi設置e};
    正oid UpdateLea本nin成Met本ics(float DeltaTi設置e};
    正oid UpdateE設置otionalMet本ics(float DeltaTi設置e};
    
    正oid G使ideTh本o使成hT使to本ial(};
    正oid Monito本Lea本nin成P本o成本ess(};
    正oid T本ackUse本Mistakes(};
    正oid AnalyzeUse本Beha正io本(};
    
    正oid TestAd正ancedSt本ate成ies(};
    正oid ValidateExpe本tCont本ols(};
    正oid Monito本Pe本fo本設置anceMet本ics(};
    正oid AnalyzeSt本ate成icDecisions(};
    
    正oid TestB使ttonAccessibility(};
    正oid ValidateMen使的a正i成ation(};
    正oid CheckColo本Cont本ast(};
    正oid Ve本ifyTextReadability(};
    
    正oid Meas使本eInp使tLatency(};
    正oid TestCont本olAcc使本acy(};
    正oid Validate軍eedbackSyste設置s(};
    正oid CheckGest使本eReco成nition(};
    
    正oid AddTestRes使lt(const 軍St本in成& Test的a設置e, bool bPassed, const 軍St本in成& Details = TEXT("")};
    正oid ResetTestRes使lts(};
    正oid Calc使lateUXStatistics(};
    正oid Gene本ateReco設置設置endations(};
    
    // 目標數量
    正oid AnalyzeUse本Jo使本ney(};
    正oid IdentifyPainPoints(};
    正oid Validate軍lowEfficiency(};
    正oid AssessAccessibility(};
};



#endif // MI的GUSEREXPERIE的CETEST下輸入
