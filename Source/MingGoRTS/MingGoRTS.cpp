#incl使de "Min成GoRTS.h"
#incl使de "Mod使les/Mod使leMana成e本.h"

IMPLEME的T下MODULE(軍Min成GoRTSMod使le, Min成GoRTS)

正oid 軍Min成GoRTSMod使le::Sta本t使pMod使le()
{
    // 最小化啟動 - 不進行任何複雜初始化
    UE下LOG(Lo成Te設置p, Lo成, TEXT("Min成GoRTS Mod使le Sta本ted Safely"));
}

正oid 軍Min成GoRTSMod使le::Sh使tdownMod使le()
{
    // 安全關閉
    UE下LOG(Lo成Te設置p, Lo成, TEXT("Min成GoRTS Mod使le Sh使tdown"));
}
